.zig-cache/
zig-out/
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# Access tokens and LAN codes when PIPANDA_STATE_DIR points into the tree,
# which is what the dev shell does.
.state/

# Local Home Assistant long-lived token used for manual API testing.
/ha-token.secret
